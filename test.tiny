func sum = (a: Integer, b: Integer) -> Integer {
    return a + b
}

func fib = (n: Integer) -> Integer {
    if n <= 1 {
        return n
    } else {
        return fib(n-1) + fib(n-2)
    }
}

var n = 1
until n >= 20 {
    IO.print("fib(" + n + ") = " + fib(n))
    n = n+1
}